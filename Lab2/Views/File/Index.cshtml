@model Lab2.Models.FileIndexModel

@{
    ViewBag.Title = "View";
}

@using (Html.BeginForm("Index", "File", FormMethod.Post))
{
    foreach (var file in Model.Files)
    {
        @Html.ActionLink(file, "Content", new { id = file });
    }
}


